# Trayce — Product Specification

更新日: 2026-09-17  
対象: Tauri版（Svelte + Rust）  
状態表記: **実装済み** / **検討中** / **対象外**

## 1. このファイラーは何か

Trayceは、複数の場所に散らばるファイルを「探す・見比べる・集める・別の場所へ渡す」作業を、画面遷移を減らして行うWindows向けデスクトップファイラーである。

一般的なファイラーの代替を網羅的に目指すのではなく、次の状態を得意領域とする。

- 右手はマウスで対象を指し、左手は少数のキーで操作する
- 複数のフォルダーや検索結果を同時に見て作業する
- コピー元を巡回しながら、必要なものを一時的に集める
- 「どこにいたか」「どこを検索したか」を見失わない
- ファイル操作を急いでも、衝突・中断・取り消しに耐えられる

短く表現すると、**フォルダー閲覧、検索、収集、受け渡しをペイン上で並行できる作業指向ファイラー**である。

### 1.1 名称と視覚的な印

製品名は **Trayce**。`Tray`（集めて受け渡す場所）と`Trace`（経路をたどって探すこと）を重ねた名称である。

アイコンは、異なる場所から来た2本の経路が合流し、下部のトレイへ収まる形を採用する。ミントと青の経路、チャコール系の背景を用い、小さいタスクバー表示でも輪郭を判別できる非対称な構成とする。

## 2. 中心となる利用像

### 2.1 想定する姿勢

- 右手: マウス操作、ポイント、選択、ドラッグ
- 左手: `Q`、`W`、`F`、`N`、`Space`など、左側へ寄せたショートカット
- キーボードだけで完結させることは主目的ではない
- マウスだけでも基本操作を失わない

### 2.2 代表的な作業フロー

#### 集めてから渡す

1. 複数のフォルダーを巡回する
2. 必要な項目を`Alt+Click`でトレイへ集める
3. 転送先ペインを表示する
4. トレイ全体または一部をコピー・移動する

#### 検索しながら整理する

1. 通常ペインを検索ペインへ変える、または検索ペインを隣に追加する
2. 検索語を入力し、結果をリアルタイムに絞る
3. 結果をプレビュー、トレイへ追加、または通常ペインへ渡す
4. 「検索した場所」から、同じ検索語のまま以前の場所を再検索する

#### 複数の仕事を保つ

1. タブで仕事のまとまりを分ける
2. タブ内のペインで、フォルダーと検索を並べる
3. 必要なら独立ウィンドウへ切り離す
4. ウィンドウ一覧／ワークベンチから別ウィンドウへ項目を渡す

## 3. 設計原則

### 3.1 ペインは「場所」だけでなく「役割」を持つ

**実装済み。** ペインには現在、次の2種類がある。

- `directory`: 通常のフォルダー表示
- `search`: 複数の実在パスを横断する検索結果表示

検索は一時的なダイアログではなく、通常ペインと並べて維持できる作業面である。将来的に比較、最近使った項目、専用トレイなどを別のペイン役割として追加できる構造を前提とする。

### 3.2 ファイラー履歴と検索履歴は並列だが別物

**実装済み。** 次の3種類を混同しない。

- フォルダー履歴: 通常ペインで訪れた場所
- 検索場所履歴: 検索対象にしたルートの組み合わせ
- 検索語履歴: その検索ペインで使用した条件

検索場所を履歴から変更しても、現在の検索語は維持する。「ここから検索した後、さっき調べた場所も同じ言葉で調べる」という流れを短くするためである。

### 3.3 選択と収集を分ける

**実装済み。** 通常選択は直近の操作対象、トレイは場所をまたいで残す一時収集物である。コピーを実行してもトレイは維持し、移動では実際に完了した項目だけをトレイから外す。

### 3.4 危険な処理は結果に基づいて状態を変える

**実装済み。** コピー・移動・削除などは、操作を試みた時点ではなく実際の完了結果に基づいてUIを更新する。衝突回避、進捗、中断、可能な操作の取り消しを重視する。

### 3.5 情報量は利用者が調整できる

**実装済み。** サイドバー、プレビュー、検索履歴欄は表示を切り替えられる。通常ペインのサイドバーは上下2段へ分割でき、各段にツリー・お気に入り・履歴・トレイを割り当てられる。

### 3.6 場所の近さは、移動のコストに表れる

**実装済み。** 一般的なファイラーは表示フォルダーを丸ごと切り替えるため、親へ戻るのも無関係な場所へ跳ぶのも同じコストになる。Trayceは次の3方向でこれを崩す。

- **戻りを無料にする**: 訪れた場所ごとにスクロール位置・選択・カーソル・絞り込み語・並べ替えを覚え、戻った時に作業の続きから再開する（ペインあたり32か所、セッションに保存）。絞り込みを復元した場所では、項目が黙って減っているように見えないよう入力欄に「復元」の印を出す
- **降りずに中を見る**: フォルダー行をその場で開いて親子を同じ一覧に並べられる。プレビュー欄はフォルダーも対象で、中身の一覧と件数・容量を出す
- **移動そのものを速くする**: 親と、見えている子フォルダーを裏で先読みして短時間だけ控える。変更通知を受けた場所は必ず捨てる。読み込みに150ms以上かかった場所では親だけに絞る（ネットワークドライブは割り当てられているとパスから見分けられないため、種別ではなく実測で判断する）。同時に投げるのは2件までで、移動したら前の場所ぶんの待ち行列は捨てる

### 3.7 場所を指す操作は、左右で意味が対になる

**実装済み。** 場所が並ぶ面では、左クリックと中クリックが対になる。

- **左クリック**: そこへ行く（いまのペインが動く）
- **中クリック**: いまのペインを動かさず、その場所を隣のペインで開く

対象は場所が出ているところすべて——一覧のフォルダー行と `..` 行、ツリー、★お気に入り、履歴、トレイの経由地、パスバーの階層と `▾` の兄弟一覧、検索結果のフォルダー。覚える規則が1つで済むように、どこでも同じ意味にする。

ブラウザのタブ中クリックからの連想だが、行き先は窓ではなく**ペイン**である。タブが担うのは「安く作れて安く捨てられる入れ物」で、Trayceでそれにあたるのはペインだから（窓は位置を持ち、閉じるのに`Alt+F4`が要る）。またタブは中身を**隠す**入れ物なので、「見比べるために開く」という目的と逆になる。

中ボタンはホイールそのもので指が滑るため、重い結果を置かない。これは`Alt+Q`／`Alt+W`で引いたのと同じ線である。誤って作ったペインは`W`一発で消えるが、誤って作った窓は位置を持って居座る。

ファイル行には割り当てない。ファイルは隣のペインでは開けず、別の動詞（トレイ追加など）を当てると規則が一行で説明できなくなる。固定（📌）したペインの上でも素直に隣へ出す——いまのペインを動かさない操作なので、固定の判定を通す理由がない。

### 3.8 ペインの並びは、作り直すものではなく呼び出すもの

**実装済み。** 複数の場所を並べた状態を作るには十数操作かかり、それが毎回失われる。Trayceはペインの並びを「配置」として名前付きで保存し、呼び出せる。

配置は絶対パスではなく**基準フォルダーからの相対**（ここ／親／子／絶対）で覚えるため、保存した時とは別の場所にも同じ形を当て直せる。

## 4. 画面と情報構造

### 4.1 タブ

**実装済み。** 1つの仕事のまとまりを保持する。各タブは複数ペインと1つの共有トレイを持つ。閉じたタブは開き直せる。

タブを切り替えても、離れるタブのペインが持っていた「場所ごとの作業状態」（スクロール位置・選択・カーソル・絞り込み・並べ替え）は引き継がれる。表に出ていないタブのペインは描画されず破棄されるため、出て行く時に明示的に引き取る。§3.6の「戻りを無料にする」はタブをまたいでも成り立たなければならない——タブは利用者が戻る単位として一番大きいものだからである。

### 4.2 通常ペイン

**実装済み。** 主な構成は以下。

- 現在地を示すパスバー
- フォルダー内の絞り込み入力
- ファイル一覧と並べ替え（フォルダー行はその場で開ける。空き領域からのドラッグで矩形選択）
- 開閉可能な左サイドバー
- 開閉可能な右プレビュー
- フォルダー内容の概要
- コピー／移動の進捗表示

ペインは📌で固定できる。固定したペインは移動せず、パスバー・一覧・ツリー・履歴・`Q`で移動しようとした先は、固定していない別の通常ペインで開く（無ければ隣に新しいペインを作る）。転送先を決めたまま、探す側だけを動かすための機能で、固定状態はセッションに保存する。

サイドバーのツリーでは、フォルダー名のクリックが移動と開閉を兼ねる。同じ名前を再クリックすると閉じる。三角ボタンは移動せず開閉だけを行う。

パスバーの上位階層は、それぞれ `▾` でその階層のフォルダー一覧を出せる。親へ上がってから選び直さずに、兄弟へ1操作で移れる。一覧の中では現在地を色で示す。

一覧のフォルダー行は `▸` または `→` でその場に展開できる。現在地は変わらないため、選択もスクロールも失わずに子を見られる。展開した子はトレイへの収集・ドラッグ・選択の対象になり、複数階層からまとめて拾える。`←` は畳む、または親の行へ戻る。場所を移ると展開は畳まれる。

`E` はこの段のフォルダーをまとめて開き、1つでも開いていればすべて畳む。開く側は先頭40件で打ち切る（展開は1フォルダーにつき1回読みに行くため、数千件を一斉に開くと本来の操作が止まる）。打ち切った時は何件開いたかを知らせる。

直前に潜っていた子を覚えており、親へ戻るとパスバーに `↘子の名前` が出る。木を行き来する時、同じ枝へ戻るのが1操作で済む。

注目していないペイン（分割中で非アクティブ）は、種類と更新日時の列を畳んで、その幅を名前へ回す。

一覧の空き領域からドラッグすると矩形選択になる。行の上から引くのは外部アプリへのドラッグなので、**起点で用途が決まる**。`Ctrl`・`Shift`を併用すると既存の選択へ足す。端まで引くと自動でスクロールし、スクロールバーを掴んだ場合は矩形にしない。

フォルダー内の絞り込みは `Ctrl+Enter` または `⌕下位も` で、同じ語のまま現在地を対象にした検索ペインへ引き継げる。1階層で見つからない時に、階層を降りる作業を検索で置き換えるための道。

### 4.3 サイドバー

**実装済み。** 表示できる情報は以下。

- ツリー
- ★ お気に入り
- フォルダー履歴
- トレイ

`↕`で上下2段に分割できる。分割後は各段を独立して切り替え、境界をドラッグして高さを調整できる。片方を閉じると1段へ戻る。構成はペインごとに復元される。

★ お気に入りには、フォルダーとファイルをドラッグして登録できる。一覧・ツリー・他のペインからでも、エクスプローラーからでも同じように落とせる。登録は常に末尾へ足す方向で、既にあるものを落としても解除にはならない。落とした結果は「n件を★へ登録 / n件はすでに登録済み」のように知らせる——何も起きないと、届かなかったのか無視されたのか区別できないためである。

パス欄の ☆ と `F` は、これまでどおり**現在地フォルダー**を対象にする。選択の有無で対象が変わることはない。特定の項目を指して登録するのはマウスの仕事で、いま立っている場所を登録するのが左手の仕事、という分担である。

#### お気に入りの2つの性格

トレイと同じく、種別を登録時に宣言させず、**種別から動詞を導く**。ただし向きはトレイと逆になる。

| | クリック | 増える動詞 |
|---|---|---|
| フォルダー | そこへ行く | — |
| ファイル | 開く | `→` 含まれる場所を開く |

`→` は普段は薄く、行に触れると濃くなる（一覧のインライン展開 `▸` やトレイの `→` と同じ作法）。当たり判定を選択と分けてあるので、掴んでの並べ替えはそのまま使える。実行ファイルは開く前に一度確認する。ファイル行には種類のアイコンが付き、色帯だけの場所行と見分けられる。

ファイルを入れられることで、★は簡易ランチャーとしても働く。ただし**検索ペインの「検索場所の候補」に並ぶのは場所だけ**である。ファイルは検索ルートになれないため、押しても走査が始まらない行を並べても選びにくくするだけになる。

ファイル行の `→` による移動は、移動の集計（Settings → 移動の集計）に数えない。この機能が生んだ跳躍を数えると、`other` の割合で俯瞰画面の要否を決める計測が自分の足で歪むためである。

登録・解除・並べ替えは、開いているすべてのペインの★欄へ同時に反映する（上下分割の下段を含む）。

### 4.4 検索ペイン

**実装済み。** 通常ペインを置き換える方法と、元ペインを残して隣へ追加する方法がある。

- ペインへ変更した時点で対象場所の読み込みを開始
- 検索語の入力中にリアルタイムで結果を更新
- Enterは検索開始ではなく検索語履歴の確定に使用
- 複数ルートを`;`または`；`区切りで指定
- 空白はAND、`|`はOR、先頭の`!`または`-`は除外
- 名前だけ／パスを含める検索を切り替え
- 一時停止・再開は読み込みだけを止める。読み込み済みの索引は残るので、止めたまま検索できる
- 停止は読み込みをやめ、読み込み済みの索引も破棄する。以後は再読み込みするまで検索し直せず、その旨を状態表示に出す（結果の表示自体は残る）
- 再読み込み
- 新しく作った検索ペインの並べ替えは更新日時の降順から始める。探す対象は直近に触ったものが大半で、名前順から入ると必ず並べ替え直すことになるため
- 検索結果の選択、複数選択、プレビュー、起動、場所表示
- 結果からトレイ、コピー、切り取り、パスコピー、ドラッグ
- 最大500件を画面へ表示し、総一致件数とは分けて示す
- 英字の大文字小文字、全角半角（英数記号・空白）、半角カナを区別しない。全角の`！`・`－`・`｜`も除外・ORとして働く
- 属性の条件を検索語に混ぜられる。先頭に`!`か`-`を付けると反転する
  - `ext:png,jpg` 拡張子
  - `size:>10mb` / `size:<1kb` 大きさ（b・kb・mb・gb）
  - `modified:24h` / `7d` / `2w` / `today` 更新日時（`today`は24時間以内）
  - `type:file` / `type:dir` 種類
  - 値が読めない場合は条件にせず、普通の検索語として扱う
- 設定の「読み込まないフォルダ名」に一致するフォルダは中へ潜らない（既定: `.git`、`node_modules`、`$RECYCLE.BIN`、`System Volume Information`）
- 読み込み後に移動・削除された項目は、ペインへ戻った時（ポインターが入る・窓が前面に来る）に結果から消える
- 結果の右クリックで、含まれるフォルダを隣に開く、トレイへ入れる／外す、フォルダに絞って検索、フォルダ内を隣で検索などを行える

通常ペインのフォルダ内絞り込み、履歴の絞り込み、ウィンドウ一覧の絞り込みも、検索と同じく大文字小文字・全角半角を区別しない。

検索履歴欄は既定で表示され、次を直接操作できる。

- 通常ファイラーと共有するお気に入り場所（★はファイルも持てるが、ここに並ぶのは場所だけ）
- 全検索ペインで共有する最近の検索場所（最大15件）
- ペイン固有の最近の検索語（最大10件）

検索ペインから検索ペインを追加すると、検索場所、検索語、並べ替え、検索条件、表示状態を複製する。追加後の2ペインは独立して変更できる。ただし索引は共有しないため、同じ場所を複製した場合はその場所をもう一度まるごと走査する（メモリもディスク走査も2倍になる）。別の場所へ変えて使う前提の機能である。

走査が終わった後に追加されたファイルは、再読み込みするまで結果に現れない。消えた項目は上記のとおりペインへ戻った時に除かれるが、増えた項目は追いかけない。

### 4.5 配置

**実装済み。** タブのペインの並びを名前付きで保存し、呼び出せる。`Ctrl+E` またはパスバーの `⊞` で開く。

- 保存時は、表に出ているペインの場所を基準にして、各ペインの場所を「ここ」「親」「子（相対名）」「絶対パス」のいずれかで覚える
- 呼び出すと、その時点で表に出ているペインの場所を基準にして展開する。保存した場所とは別の場所にも同じ形を当てられる
- 検索ペインは種類と検索語ごと復元する。固定（📌）も復元する
- 呼び出しは現在のタブを置き換える。呼ぶたびにタブが増えると、増えたタブを閉じる操作が新たなコストになるため
- 一覧の並び順がそのまま `Ctrl+1`〜`Ctrl+9` の割り当てで、一覧から並べ替えられる
- 名前だけでは思い出せないため、一覧には形（`親 │ ここ │ 🔍検索`）を併記する
- 同じ名前で保存すると置き換える。削除しても一覧から消えるだけで、フォルダーには触れない

### 4.6 トレイ

**実装済み。** タブに属する一時収集領域。

- 1つのタブに名前付きのトレイを複数持てる（「納品用」「確認待ち」など）。トレイ欄の先頭で切り替え・追加・名前変更・削除を行う。削除しても集めた一覧が消えるだけで、ファイルには触れない
- `Alt+Click`で追加・解除（右クリックメニューからも可能）。対象は選んでいるトレイ
- `C`でも選択項目を追加・解除できる。選択が全部入っていれば外し、1つでも入っていなければ入れる（混在時に一部だけ外れる事故を避ける）。集めるのが左手の仕事である以上、集める操作だけマウスを要求しない
- 通常クリック、`Ctrl+Click`、`Shift+Click`でトレイ内を部分選択
- 選択部分または全体を現在の通常ペインへコピー／移動
- 存在しなくなったパスも警告付きで残し、利用者が明示的に外す
- セッション復元に対応

#### トレイの2つの性格

トレイに入る項目は、種別によって役割が違う。

- **ファイル＝運ぶ荷物。** 動詞はコピー・移動で、届ければ役目が終わる
- **フォルダ＝経由地。** 動詞は「行く」で、その探し物が終わるまで残る

どちらなのかを登録時に宣言させてはいけない。拾う瞬間の利用者は探している最中で、分類している余裕がない。`Alt+Click`が1操作で済むという長所が失われる。種別からほぼ導けるので、**フォルダには動詞を1つ増やす**という形で扱う。

- フォルダ行には `→`（ここへ移動）が出る。普段は薄く、行に触れると濃くなる（一覧のインライン展開 `▸` と同じ作法）
- ダブルクリックは、フォルダなら中へ入り、ファイルなら含まれる場所を開く
- `→` は選択とは当たり判定を分けてあるので、部分選択はそのまま使える
- 行き先になれるフォルダは、アイコンの色を荷物と分ける

これにより、トレイは**捨てる前提の一時的なブックマーク**として使える。★お気に入りとは寿命と範囲が違う。

| | ★ お気に入り | トレイ（経由地として） |
|---|---|---|
| 寿命 | 長期 | いまの仕事の間だけ |
| 登録 | 狙って置く（☆・`F`・欄へドラッグ） | `Alt+Click` 1操作 |
| 範囲 | 全ウィンドウ共有 | タブに属する |

トレイが勝つのは速さではなく、**汚れないこと**である。今日だけ使う十数個の場所を★に入れると★が使い物にならなくなるが、捨てる前提の置き場があればためらわずに拾える。

フォルダとファイルが混ざると「すべてコピー」が経由地まで運ぶ危険があるため、概要欄に内訳（フォルダ n / ファイル n）を出し、押す前に見えるようにする。ハードに分けたい場合は複数トレイを使う。

### 4.7 複数ウィンドウとワークベンチ

**実装済み。** ペインを独立ウィンドウへ切り離せる。`Ctrl+Shift+Space`で開いているTrayceウィンドウの一覧を呼び出し、ワークベンチ表示ではウィンドウ間のドラッグ転送を扱う。

- ドロップは既定でコピー
- Shiftを押したドロップは移動
- 進捗、中止、重複操作防止
- 実際に移動できた項目だけ元トレイから除去

#### 直前のウィンドウへ戻る

**実装済み。** `Alt+Q` で、直前まで使っていたTrayceウィンドウと入れ替わる。もう一度押すと元へ戻る。

素の `Q`・`W` がペインに効くのに対し、`Alt` を足すと同じ指のまま一段外側の「ウィンドウ」に効く、という規則である。ウィンドウの層には破壊的な操作を置かない。切り替えキーの隣で指が滑った時に、ペイン1枚ではなくウィンドウごと失うことになるためである（ウィンドウを閉じるのはタイトルバーと `Alt+F4` に任せる）。

入れ替えるのは**前面と操作対象だけ**で、ウィンドウの位置・大きさ・中身は動かさない。配置を組むのは利用者の仕事であり、切り替えキーがそれを毎回書き換えてはならない。

ただし、ウィンドウが重なっている場合は前面化しても見た目が変わらないことがある（切り替えた先が元のウィンドウを覆うだけになる）。そのため、キーで移ってきたウィンドウは縁を一瞬光らせる。色はそのウィンドウの色帯と同じものを使うので、光った色自体が「どこへ来たか」を含む。クリックで前面に来た時は光らせない——自分で押した場所は分かっているため。

`Alt` 修飾は文字を生まないため、絞り込み欄や検索語欄に入力中でも効く。探している最中こそ隣のウィンドウを見たくなるためである。

ウィンドウが1つしかない時は何も起こさない。押し間違えるたびに通知を出すと、キーを覚える途中で邪魔になる。

#### 並んだ順に巡回する

**実装済み。** `Alt+W` で次のウィンドウへ、`Alt+Shift+W` で前のウィンドウへ移る。端まで行くと反対の端へ巻き戻る。

`Alt+Q` と役割を分けてあるのは、**拠り所にする並びが違う**ためである。

| | `Alt+Q`（往復） | `Alt+W`（巡回） |
|---|---|---|
| 並びの基準 | 最後に前面へ来た順 | 画面上の位置（左→右、同列なら上→下） |
| 押すと並びが | 変わる | 変わらない |
| 得意なこと | 2つの間を行き来する | 3つ以上を漏れなく回る |

往復の並びは使うたびに組み変わるので、2回押すと元の場所へ戻ってしまい「2つ先」へは行けない。巡回の並びは押しても動かないので、3枚開いていれば `Alt+W` を3回で全部に寄れる。この2つを同じキーに混ぜると、戻るつもりで進む事故になるため、別々に割り当てる。

巡回で到着した時だけ、縁の点滅に加えて「2/3 番目のウィンドウ」と位置を知らせる。往復では出さない——2枚を行き来している間、`1/2` と `2/2` が交互に出るだけで何も伝えないためである。

3つ以上のウィンドウを名前や中身で狙って選ぶのは、引き続き `Ctrl+Shift+Space` のウィンドウ一覧の仕事である。

最後の通常Trayceウィンドウを閉じると、非表示オーバーレイだけを残さずプロセスを終了する。

Trayceは同時に1つだけ起動する。起動中にもう一度起動すると、新しいプロセスは立ち上がらず、直近に使っていたウィンドウが前面に出る（ウィンドウ一覧や保存データが2つに分かれて食い違うのを防ぐため）。

コンパクトなリスト表示では、ウィンドウの下に現在タブの全ペインを子行として表示する。通常／検索の種類、パス、検索語、アクティブ状態を識別でき、子行を選ぶと対象ウィンドウを前面に出してそのペインをアクティブにする。別タブは展開せず件数だけを示し、一覧の過密化を避ける。

俯瞰ワークベンチでは、各ウィンドウカードの内部に全タブの名前を横スクロール可能なタブ列として表示し、その下に現在タブのペイン地図を表示する。タブ名を選ぶと実際のTrayceもそのタブへ切り替わり、ペイン地図とカード本体が追従する。ペイン区画を選ぶと対象ペインをアクティブにし、カード本体もそのペインの内容へ切り替わる。通常ペインは区画ごとにコピー／移動のドロップ先となり、同じウィンドウ内の別ペイン間でも転送できる。検索ペインは検索語と場所を示すが、誤操作防止のため転送先にはしない。

## 5. ファイル操作

**実装済み。** 現在扱う主な操作。

- ファイル／フォルダーを開く
- コピー、切り取り、貼り付け
- 名前変更
- 新規フォルダー作成
- ゴミ箱へ送る
- ZIP圧縮、ZIP展開
- フルパスをコピー
- エクスプローラーで場所を表示
- 外部アプリへのネイティブドラッグ
- 直前の対応操作を元に戻す

同名項目を黙って上書きしない。転送はバックグラウンドで実行し、進捗と中断を扱う。

転送先に同名の項目がある場合は、転送を始める前に選択肢を出す。

- 両方残す: 新しい方を`名前 (2)`にする（既定、Enter）
- 上書き: 既存をゴミ箱へ送ってから置く（ゴミ箱から戻せる）
- スキップ: 同名の項目は転送しない（トレイからも外れない）
- キャンセル（Esc）: 何も転送しない。切り取りも残る

コピーでは更新日時を元のファイルから引き継ぐ。同じドライブ内の移動は中身を複製せず名前の付け替えで行う。中断した項目は完了扱いにせず、途中まで作ったものは残さない。

通常ペインの右クリックからは、フォルダ内を隣のペインで検索することもできる。

上書きを選んだ転送を`Ctrl+Z`で取り消すと、新しい方を戻したうえで、ゴミ箱へ送った既存項目も元の場所へ復元する。

転送中に次のコピー・移動を始めると、そのペインの順番待ちに入り、今の転送が終わると順に始まる。転送バーに待ち件数を表示し、待ちだけを取り消せる。衝突の扱いは依頼した時点で選ぶ。

#### 一括名前変更

選択した項目の名前をまとめて変える。一覧の並び順で連番を振る。

- 探す／置換（既定は大文字小文字を区別しない）
- 名前の形: `{name}`（置換後の元の名前）と`{n}`（連番。開始値と桁数を指定）
- 大文字・小文字への統一
- 拡張子は変えない

適用前に新しい名前を一覧で示し、不正な名前・同じ名前の重複・既存との衝突があれば適用できない。途中で失敗したらすべて元に戻し、1回の`Ctrl+Z`でまとめて戻せる。

#### フォルダの大きさ

一覧ではフォルダの大きさを表示しない（開くたびに中身を全部読まないため）。右クリックの「大きさを数える」で、必要な時だけファイル数と合計サイズを数える。

## 6. 主な既定ショートカット

ショートカットは設定画面から変更でき、重複を検出する。

| 操作 | 既定キー |
|---|---|
| ポインター先で親へ | `Q` |
| ポインター先のペインを閉じる | `W` |
| ポインター先をお気に入り切替 | `F` |
| ポインター先を通常分割 | `N` |
| ポインター先に検索ペインを追加 | `Shift+N` |
| ポインター先のプレビュー（フォルダーは中身一覧） | `Space` |
| 名前／サイズ／種類／更新日時で並べ替え | `A` / `S` / `X` / `Z` |
| 並び順を反転（軸は変えない） | `D` |
| 全ペインを同じ並びに揃える | `Shift+A` / `Shift+S` / `Shift+X` / `Shift+Z` |
| その場でフォルダーを開く／畳む | `→` / `←` |
| その場展開をすべて開く／畳む | `E` |
| 選択をトレイに入れる／外す | `C` |
| 場所を隣のペインで開く | マウス中クリック |
| 隠しファイルの表示切替 | `R` |
| 絞り込みを下位の検索へ引き継ぐ | `Ctrl+Enter` |
| 配置を開く | `Ctrl+E` |
| 保存した配置の n 番目を当てる | `Ctrl+1`〜`Ctrl+9` |
| パスバーへ | `Ctrl+L` |
| フォルダー内を絞り込み | `Ctrl+F` |
| コピー／切り取り／貼り付け | `Ctrl+C` / `Ctrl+X` / `Ctrl+V` |
| 元に戻す | `Ctrl+Z` |
| 名前変更 | `F2` |
| ゴミ箱へ | `Delete` |
| 新規フォルダー | `Ctrl+Shift+N` |
| ZIP圧縮 | `Ctrl+Shift+Z` |
| フルパスをコピー | `Ctrl+Shift+C` |
| 新規／終了／復元タブ | `Ctrl+T` / `Ctrl+W` / `Ctrl+Shift+T` |
| 次／前のタブ | `Ctrl+Tab` / `Ctrl+Shift+Tab` |
| ウィンドウ一覧 | `Ctrl+Shift+Space` |
| 直前のウィンドウへ | `Alt+Q` |
| 次／前のウィンドウへ（画面の並び順） | `Alt+W` / `Alt+Shift+W` |

単独キーによる左手操作は、マウスポインター下のペインを優先し、なければアクティブペインへ作用する。文字入力欄では入力を優先する。

並べ替えのキーは、同じキーをもう一度押すと昇順・降順が反転する。更新日時とサイズは降順（新しい方・大きい方が先）から入る。フォルダーは常に上に固まり、フォルダーはフォルダーの中で、ファイルはファイルの中でそれぞれ並ぶ。

`Shift+` を伴う並べ替えは、そのタブの通常ペインすべてを同じ並びに揃える。向きは押したペインで決めてから配るので、ペインごとに反転してばらけることはない。検索ペインは対象外（結果の並びは検索条件の側に属するため）。

検索ペインでも並べ替えとトレイのキーは同じように効く。`E`（その場展開）と `R`（隠しファイル）は検索結果では意味を持たないため割り当てていない。

## 7. 保存される状態

**実装済み。** 主に以下を保存・復元する。

- タブとペイン構成
- ペインの役割と最後のフォルダーパス
- 検索場所、検索語、検索オプション、検索語履歴
- サイドバーの上下構成と高さ
- 検索履歴欄とプレビューの表示状態
- タブごとのトレイ
- ペインごとの「場所ごとの作業状態」（最大32か所。スクロール位置・選択・カーソル・絞り込み語・並べ替え）
- 名前付きの配置（ペインの並びの型）
- お気に入り
- フォルダー履歴
- 検索場所履歴
- ショートカットと一般設定

その場での展開状態と先読みの控えは保存しない。どちらも現在地に紐づく一時的なもので、復元すると場所と食い違う。

検索結果そのものは保存せず、復元時に条件から再構築する。古い保存データで新しい項目が欠けていても、安全な既定値で起動する。

タブ・ペイン構成とトレイは、切り離したウィンドウを含むすべてのウィンドウが保存する。次回起動時は**最後に閉じたウィンドウ**の状態をメインウィンドウに復元する（複数ウィンドウの同時復元は現時点では行わない）。

## 8. 内部構成と非機能要件

### 技術構成

- UI: Svelte 4 + TypeScript + Vite
- デスクトップ基盤: Tauri 2
- ファイル操作、検索、永続化: Rust
- 対象OS: 現時点ではWindows中心

### 検索

- Rust側に全件キャッシュと正規化済み名前／パスを保持
- 専用ワーカーで絞り込み
- 入力が連続した場合は古い要求を捨て、最新条件を優先
- リクエストIDで古い結果の混入を防止
- 走査中の更新通知を件数に応じて間引く
- 一時停止中も読み込み済みデータの検索は可能

### 応答性

**実装済み。** 「待たされない」は機能ではなく前提として扱う。次を守る。

- **ディスクに触るコマンドはメインスレッドで走らせない。** Tauriは`async`でないコマンドをメインスレッドで実行するため、読み込みの間UIが止まる。Trayceは1プロセスで複数のウィンドウを持つので、止まるのはそのペインだけでなく**すべてのウィンドウ**になる。`list_dir`、`path_kinds`、`list_subdirs`、`preview_entry`、`complete_path`などは`#[tauri::command(async)]`とする（クリップボード・シェル・ゴミ箱はスレッド親和性があるため対象外）
- **変わらないものを繰り返し畳まない。** 大文字小文字・全角半角を畳む処理は、一覧・索引に対して**1件につき1度**行い、打鍵ごとに全件へ掛けない。畳む対象を含まない文字列は小文字化だけで済ませる
- **表示していない情報を取りに行かない。** サイドバーは開いている欄のぶんだけ取り直す。とくに実在確認は1件につき最大2回のstatになるため、描画の必要が生じた時だけ行う
- **同じ内容をIPCで送り直さない。** ウィンドウ情報の同期は1目盛りに1回へ畳み、内容が変わった時だけ送る
- ウィンドウタイトルの書き手はフロント側の1箇所だけとする

測定の目安（`C:\Windows\WinSxS` 30,738件、release）: 一覧の読み込み40ms、IPCペイロード5.5MB、絞り込み1打鍵1ms未満。

### 安全性

- 既存ファイルを暗黙に上書きしない（上書きは利用者が選んだ場合だけで、既存はゴミ箱へ送る）
- キャンセル時に不完全なファイルを残さない
- フォルダーを自分自身の内部へ移動しない
- ZIP展開時のパストラバーサルを防止
- 保存データは一時ファイルへ書いてから置き換える
- 完了イベントが開始応答より先に届く競合を考慮する

### 品質確認

変更時の基本確認は以下。

- Svelte／TypeScript診断
- フロントエンドテスト
- Rustテスト
- 本番用Webビルド
- 差分の空白・改行異常確認
- 実機でのTauri操作確認（必要な機能）

## 9. 検討中の方向性

ここは要件ではなく、ヒアリング候補である。

### 優先候補

ペインの固定、複数トレイ、属性による検索条件、配置（ペインの並びの型）、★へのドラッグ登録とファイル登録は実装済み（上記）。

1. 比較ペイン
   - 左右にしかない項目、サイズ、日時、内容差を可視化する
2. 保存検索／仮想フォルダー
   - `modified:today`、`size:>100mb`、`ext:png,jpg` のような条件に名前を付けて再利用する
3. 複数ウィンドウの同時復元
   - 現在は最後に閉じたウィンドウだけを復元する
4. タスクトレイ常駐
   - `✕` でウィンドウを閉じてもプロセスを残し、タスクトレイから明示的に終了するまで生かす
   - **主たる利点は「呼び戻しが速い」ではなく、上の3を作らずに済むこと。** 窓を3つ開いて全部閉じても、常駐していれば全部そのまま生きている
   - グローバルホットキーはプロセスが死ぬと登録も消えるため、「どこからでも呼び戻す」は常駐でしか成立しない。ただしその時の `Ctrl+Shift+Space` は俯瞰ではなく**召喚**として働く（窓が0枚なら俯瞰する対象が無い）
   - 一度「見えない常駐」をバグとして潰した経緯がある。タスクトレイに姿を出すことが、当時との違いになる
   - 実装時に必ず要るもの: 二重起動時に「窓が無ければセッションを復元する」を通すこと（通さないとアイコンを押しても何も起きないアプリになる）、最後の窓を閉じた時点での `state.json` フラッシュ（終了時フラッシュが走らなくなるため）、検索索引の破棄、debug ビルドでは常駐しないこと（実行ファイルがロックされ、自身のビルドが落ちる）
   - 着手は下の計測の後。プロセスの寿命を変えるとセッションの区切りが変わり、通算で読む集計が濁る
5. 時間軸のペイン
   - 「あれはどこか」の手掛かりは場所より時刻であることが多い。場所を跨いで、今日／昨日／先週に触れた項目を並べる面。フォルダー履歴と更新日時から組み立てられる
6. 渡し先としての★
   - 選択項目やトレイを、★に登録した実行ファイルの行へドロップすると、そのアプリで開く
   - 「集めて渡す」の渡し先が、いまはフォルダーしか無い。**アプリを行き先の一種として見る**と、ランチャーを足すのではなく行き先の種類が増える話になり、既存の選択・トレイ・ドロップの受け皿にそのまま乗る
   - 実行前確認と「プログラムから開く」は実装済みなので、必要なのは配線と、複数ファイルを引数に渡す時の上限判断
7. 俯瞰ナビゲーター
   - 現在地の祖先と子孫を一枚の地図に描き、遠い場所への跳躍と、移動せずに深い場所へ置く操作に使う。ただし地図を読む時間は3操作ぶんに相当するため、近距離の移動を担わせない線引きが要る。採否は下の計測で判断する

### 計測して判断する候補

- 移動種別の偏り（**計測は実装済み**）
  - 移動先が親／子／孫／兄弟／それ以外のどれかを数え、あわせて30秒以内の出戻り回数も数える
  - 累計は保存され、窓を閉じても続きから数える。**設定画面の「移動の集計」**で割合と判断の目安を読める。25回ごとにログへも要約を出す（`[nav]` 行）
  - ログは debug では標準出力、release ではログファイルへ書く。日常使いは release なので、そこで貯まらなければ判断材料にならない
  - **それ以外**（＝跳躍）の割合が小さければ、俯瞰のような跳躍向けの装置に投資しても回収できない。3割を超えるなら作るべき
  - **出戻り**が多ければ、移動を速くするより「入らずに覗く」ほうが効く
  - この判断は議論では決まらないため、実データで決める
- 検索時の並列ディレクトリー走査
  - ストレージへの負荷も増えるため、実機計測で効果が確認できた場合に採用する

## 10. 現時点の非目標

- Slint版との完全な機能同期
- OS標準ファイラーの全機能を網羅すること
- クラウドストレージやFTPの独自クライアント化
- ファイル内容全文の常時インデックス検索
- 検索結果そのものの永続保存

Slint版は別の試作であり、この文書の実装済み表記はTauri版に対するものとする。

## 11. ヒアリングで確認したい問い

このファイラーを人やAIと検討する際は、機能名より先に以下を聞く。

### 日常の流れ

- どの作業で、同じ2～3か所を繰り返し往復するか
- ファイルを探した後、最終的に何をすることが多いか
- コピー元とコピー先のどちらを頻繁に動かすか
- 一時的に集めたファイルを、どの単位で分類したいか
- 「さっきの場所」を見失うのは、移動時か検索時か

### 操作感

- 左手で無理なく押せるキーはどこまでか
- 単独キーが入力と衝突して困る場面はあるか
- マウスポインター下を操作対象にする挙動は予測しやすいか
- 画面へ常時見せたい情報と、必要時だけ見たい情報は何か
- 2ペイン、3ペイン、複数ウィンドウのどこから認知負荷が増えるか

### 安全性

- コピー／移動前に何が見えれば安心できるか
- 名前衝突時は、自動改名、確認、スキップのどれを望むか
- 取り消したい操作はどこまでか
- トレイから項目が自動で消えてよい条件は何か
- ★に入れたファイルは、どのくらいの期間で捨てたくなるか（場所と同じ寿命か、もっと短いか）

### 次機能の評価

- 比較ペイン、保存検索、時間軸のペイン、タスクトレイ常駐、渡し先としての★のどれが日々の時間短縮につながるか
- その機能を週に何回使いそうか
- 既存ツールで代替すると何手かかるか
- 便利さのために増えてもよい画面要素はどこまでか

## 12. 他AIへ渡す際の依頼テンプレート

この文書を読ませた上で、目的に応じて次のように依頼する。

### 製品レビュー

> SPEC.mdの「実装済み」と「検討中」を区別して読んでください。このファイラーの中心思想を維持したまま、日常利用で詰まりそうな操作を指摘してください。機能数ではなく、右手マウス＋左手キーボード、複数ペイン、検索・収集・転送の流れを評価軸にしてください。

### ヒアリング設計

> SPEC.mdを前提に、実際のファイル整理作業を観察するための質問を作ってください。抽象的な好みではなく、直近の具体的な作業、移動元、検索方法、転送先、失敗経験を聞き出してください。

### 新機能提案

> SPEC.mdの未実装機能を勝手に実装済みと解釈しないでください。既存のペイン役割、トレイ、検索履歴、取り消し基盤を再利用できる案を優先し、導入効果、操作手順、認知負荷、安全上の懸念を比較してください。

### 実装相談

> SPEC.mdとHANDOVER.mdを読み、既存動作と保存互換性を壊さない最小の実装単位を提案してください。各段階で独立してテスト・コミットできるように分け、推測した仕様は明示してください。

## 13. 関連資料

- `SPEC.md`: 製品の目的、利用像、現在の仕様、検討事項（この文書）
- `HANDOVER.md`: 実装経緯、技術的判断、進捗、コミット記録
- `E:\CODE\Antigravity\File_Search_APP`: 検索体験と内部方式の参考実装
- `E:\CODE\Antigravity\Py\_FILE\ChainFlowFiler\_v23\_7`: 収集・連鎖的ファイル操作の参考実装
